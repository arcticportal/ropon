	    @if (width < 576) {
	    <div class="row blue">
	      <div class="col-auto">Regions</div>
	      <div class="col">{{network.regions.join(', ')}}</div>
	    </div>
	    <!-- <div class="row blue">
	      <div class="col-auto">Subregions</div>
	      <div class="col">{{network.subregions.join(', ')}}</div>
	    </div> --> }
	    @else {
	    <div class="row blue">
	      <div class="col-auto">Regions</div>
	      <div class="col-auto region">
		{{network.regions.join(', ')}}
	      </div>
	      <!-- <div class="col-auto">Subregions</div>
	      <div class="col">{{network.subregions.join(', ')}}</div> -->
	    </div> }


	    <!-- <dl class="mb-0">
	      <span class="d-flex">
		<dt>Regions</dt>
		<dd class="region ms-2">{{
		  network.regions.join(', ')}}</dd>
	      </span>
	      <span class="d-flex">
		<dt>Subregions</dt>
		<dd class="region ms-2">{{
		  network.subregions.join(', ')}}</dd>
	      </span>
	      <span class="d-flex">
		<dt>Domains</dt>
		<dd class="domain">
		  <span *ngFor="let domain of network.domains"
			class="badge me-1">{{domain}}</span>
		</dd>
	      </span>
	      <span class="d-flex">
		<dt>Disciplines</dt>
		<dd class="discipline">
		  <span *ngFor="let discipline of network.disciplines"
			class="badge me-1">{{discipline}}</span>
		</dd>
	      </span>
	      <span class="d-flex">
		<dt>Asset Types</dt>
		<dd class="asset-type">
		  <span *ngFor="let assetType of network.asset_types"
			class="badge me-1">{{assetType}}</span>
		</dd>
	      </span>
	      <span class="d-flex">
		<dt>Website</dt>
		<dd class="ms-2 mb-0">
		  <a [href]="network.website_url" target="_blank">{{
		    util.formatUrl(network.website_url)}}</a>
		</dd>
	      </span>
	    </dl> -->
	    <!-- <table class="table table-sm table-borderless mb-0">
	      <colgroup>
		<col />
		<col />
		<col />
		<col />
	      </colgroup>
	      <tbody class="lh-1">
		<tr>
		  <td>Regions</td>
		  <td class="region">
		    <span class="badge me-1">{{
		      network.regions.join(', ')}}</span>
		  </td>
		  <td>Subregions</td>
		  <td class="region">
		    <span class="badge me-1">{{
		      network.subregions.join(', ')}}</span>
		  </td>
		</tr>
		<tr>
		  <td>Domains</td>
		  <td colspan="3" class="domain">
		    <span *ngFor="let domain of network.domains"
			  class="badge me-1">{{domain}}</span>
		  </td>
		</tr>
		<tr>
		  <td>Disciplines</td>
		  <td colspan="3" class="discipline">
		    <span *ngFor="let discipline of network.disciplines"
			  class="badge me-1">{{discipline}}</span>
		  </td>
		</tr>
		<tr>
		  <td>Asset Types</td>
		  <td colspan="3" class="asset-type">
		    <span *ngFor="let assetType of network.asset_types"
			  class="badge me-1">{{assetType}}</span>
		  </td>
		</tr>
		<tr>
		  <td>Website</td>
		  <td colspan="3">
		    <a [href]="network.website_url" target="_blank">{{
		      util.formatUrl(network.website_url)}}</a>
		  </td>
		</tr>
	      </tbody>
	    </table> -->
