# /workspace/deploy/docker-compose-dev.yml

services:
  frontend:
    image: arcticportal.azurecr.io/ropon/angular:dev
    environment:
      - ANGULAR_ENV=${ANGULAR_ENV:-dev} # just a placeholder
   
  backend:
    image: arcticportal.azurecr.io/ropon/wagtail:dev
    command: ./docker-entrypoint.sh
    environment:
      - DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE}
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}
      - DJANGO_SUPERUSER_USERNAME=${DJANGO_SUPERUSER_USERNAME}
      - DJANGO_SUPERUSER_PASSWORD=${DJANGO_SUPERUSER_PASSWORD}
      - DJANGO_SUPERUSER_EMAIL=${DJANGO_SUPERUSER_EMAIL}
      - DJANGO_ALLOWED_HOSTS=${DJANGO_ALLOWED_HOSTS}
      - CSRF_TRUSTED_ORIGINS=${CSRF_TRUSTED_ORIGINS}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
      - WAGTAILADMIN_BASE_URL=${WAGTAILADMIN_BASE_URL}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_PORT=${POSTGRES_PORT}
      - MEDIA_ROOT=${MEDIA_ROOT}
      - DJANGO_PORT=${DJANGO_PORT:-8000}
      - GUNICORN_PORT=${GUNICORN_PORT:-8000}
      - GUNICORN_WORKERS_PER_CORE=${GUNICORN_WORKERS_PER_CORE:-}
      - GUNICORN_MAX_WORKERS=${GUNICORN_MAX_WORKERS:-}
      - GUNICORN_TIMEOUT=${GUNICORN_TIMEOUT:-}
      - GUNICORN_GRACEFUL_TIMEOUT=${GUNICORN_GRACEFUL_TIMEOUT:-}
      - GUNICORN_THREADS=${GUNICORN_THREADS:-}
      - GUNICORN_LOG_LEVEL=${GUNICORN_LOG_LEVEL:-}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - backend

  db:
    image: postgis/postgis:17-3.5
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_PORT=${POSTGRES_PORT}
    networks:
      - backend
    healthcheck:
      test: [CMD, pg_isready, -U, postgres]
      interval: 5s
      timeout: 5s
      retries: 3

volumes:
  postgres_data:

networks:
  backend:
