# /workspace/deploy/frontend/docker-compose-dev.yml
version: '3.8'
services:
  frontend:
    image: arcticportal.azurecr.io/ropon/frontend:dev 
    environment: # for any dev-specific environment variables
      - ANGULAR_ENV=${ANGULAR_ENV:-dev} # just a placeholder
  